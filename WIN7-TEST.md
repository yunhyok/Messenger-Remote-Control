# Master 현장 확인: 자연 발생 분할 회신

**0.3.8 확인(Master를 v0.3.8로 설치한 뒤):** 0.3.8은 회신 직전 증거의 유효기간과 중단 뒤 재개만 바꿉니다. 대화 기록이 길어진 상태에서 Master를 밤새 켜 두고, 다음 날 평소처럼 `pwrsi`를 한 번 보내 주세요. 기대 동작은 회신이 도착하는 것입니다. 로그에서는 다음만 확인해 주세요. ① `ROUNDTRIP_PROOF_EXPIRED_OR_WINDOW_CHANGED`가 한 건도 없어야 합니다. 있으면 그 행이 남긴 시각과 직전 `READ_ONLY_PROBE_RESULT`의 `elapsed_ms`를 알려 주세요. ② `STATUS_REQUEST_ABORTED`가 있으면 그 행의 `reason`·`input_attempted`·`resumed`와 몇 번 나왔는지 알려 주세요(회신 없이 새 `Master Ready`가 한 번 더 온 경우가 여기에 해당합니다). `STATUS_REQUEST_ABORT_LIMIT`로 세션이 끝났다면 그 사실도 알려 주세요. ③ 0.3.7 항목인 `READ_ONLY_PROBE_RESULT`의 `elapsed_ms`·`children_ms`·`children_queries`·`nodes`(그리고 `read_failures`·`complete`)도 함께 적어 주세요. 요청이 없으면 원문 로그·스크린샷은 올리지 말고 위 값만 알려 주세요.

**0.3.7 확인(Master를 v0.3.7로 설치한 뒤):** 0.3.7은 대화창을 읽는 방식만 바꿉니다. 평소처럼 `pwrsi`를 한 번 보내고, 로그에서 다음 세 가지만 확인해 주세요. ① `READ_ONLY_PROBE_RESULT`의 `elapsed_ms`와 새로 추가된 `children_ms`·`children_queries`, 그리고 같은 행의 `nodes`(0.3.6에서는 같은 스냅샷이 593~632 노드에 6.3~7.7초였으므로 세 값을 함께 적어 주세요). 같은 행의 `read_failures`가 0이 아니거나 `complete=false`이면 그 값도 알려 주세요. ② 폴링 대기마다 1행씩 남는 `RECEIVE_CHANGE_TRIGGER`의 `reason` 분포(`TAIL_CHANGED`/`PERIODIC`/`NOT_IDLE`). ③ 명령을 보낸 뒤 `Master Ready`부터 처리 시작 안내까지 걸린 시간과 답장 부분 하나에 걸린 시간(0.3.6에서는 부분당 약 14초, 인식은 도착 후 스냅샷 약 2.5회였습니다). 요청이 없으면 원문 로그·스크린샷은 올리지 말고 위 값만 알려 주세요.

**0.3.6 결과(참고):** 0.3.6 첫 로그에서 서명 검증 캐시(`PROCESS_IDENTITY signature_cached`), 자기 클릭을 뺀 입력 대기(부분 사이 `WAITING_FOR_PC_IDLE` 기록 없음), 명령 대기 중 구조 표본(`RECEIVE_CHANGE_TRIGGER`)은 의도대로 동작했고 답장 부분이 약 17.7초에서 약 14.1초가 되었습니다. 화면을 한 번 읽는 시간은 줄지 않았고, 그 원인(형제 요소 이동)이 0.3.7의 변경입니다. 같은 로그의 ERROR 2건은 운용자 Stop이며 결함이 아닙니다.

**0.3.5 확인(Master를 v0.3.5로 설치한 뒤):** Start 후 창 선택 뒤 상태 표시가 "PC 입력이 끝나기를 기다리고 있습니다"에서 넘어가지 않으면 2초 뒤 표시되는 원인 문구(입력 계속 감지 / 키·버튼 눌림 / 앞 창 메뉴·끌기 / 활성 창 없음)를 그대로 기록하고, 60초 뒤 `TARGET_PC_NOT_IDLE`로 멈추면 로그의 `OPERATIONAL_TARGET_IDLE_WAIT` 행만 보내 주세요. Ready가 정상 발송되면 아래 0.3.4 항목을 이어서 확인합니다.

**0.3.4 추가 확인(Master를 v0.3.4로 설치한 뒤):** Start 뒤 남은 초가 5→1로 표시되고 선택 직후 "선택한 창: KI-Messenger (PID …)"가 표시되는지, 메신저에 `Master Ready. help, total status, pwrsi 중 하나를 보내세요. [번호]`가 도착하는지, `help` 답장이 세 줄인지 확인합니다. 명령을 보낸 뒤 대화 기록을 위로 스크롤한 상태로 두면 화면에 "명령 메시지를 읽었지만 화면 밖…" 안내가 나오고, 다시 아래로 스크롤하면 접수되는지 한 번만 확인합니다(다른 명령을 추가로 보내지 마세요). 중단이 생기면 사유 코드 옆의 한국어 설명과 다음 행동 문구, 로그의 `OPERATIONAL_TARGET_DENIED`/`RECEIVE_READY_NOT_OBSERVED` 유무만 기록합니다.

## v0.3.3 기준 절차

비공개 v0.3.1 현장 기록은 10개 부분 중 여섯 부분 뒤 일곱 번째 전 `RECEIVE_READY_BOUNDARY_CHANGED`에서 멈췄습니다. Ready 문구 또는 화면 구조가 정확히 어떻게 바뀌었는지는 알 수 없고 idle timeout이나 crash도 확인되지 않았습니다. v0.3.3은 그 재확인 경로와 창 사용 방식을 수정하지만, 열 부분 전체 성공을 아직 주장하지 않습니다.

1. **Master만 v0.3.3으로 설치**합니다. Slave v0.3.0/0.3.1/0.3.2와 기존 설정·송부 이력은 유지합니다.
2. Start 후 5초 안에 사용할 대화창을 한 번 클릭합니다. Ready가 오면 마우스를 다른 곳으로 옮겨도 됩니다. 선택한 메신저 창을 **최소화하지 않은 채** 다른 일반 창 뒤에 둡니다. 다음 평소 `pwrsi`에서 자연스럽게 분할 회신이 생기면 그대로 둡니다. 마지막 `N/N`과 다음 `Master Ready`만 확인합니다.
3. 나중에 평소 명령을 한 번 보내 정상 접수·회신을 확인합니다.

마지막 회신 입력 직전에 선택한 창이 한 번 앞으로 올 수 있습니다. 새 명령의 최초 접수에는 보이는 enabled 전체 메시지가 필요합니다. 이미 접수한 요청의 재확인에서만 이전 Ready 문구를 과거 기록으로 취급합니다. 접수 명령·enabled 상태·요소·이력 행 순서 또는 대화가 바뀌면 전송을 중단합니다. 불명확한 전송은 자동 재시도하지 않고, 모든 부분의 보호된 전송이 완료되기 전에는 송부 이력을 갱신하지 않습니다.

이력 초기화, PowerSI/HFSS 재시작, 몇 시간 대기 시험은 하지 마세요. 설치·제거는 설정과 이력을 보존합니다. 자동 검사 결과와 실제 환경 검증의 구분은 [HANDOFF.md](HANDOFF.md)에 기록합니다.

로컬 Windows 10에서 v0.3.3 Release 빌드, 두 실제 EXE 자체 검사, 두 설치파일 생성·검사 및 Master 설치·재설치·제거·설정 보존 검사를 통과했습니다. 별도의 WPF 시험 창에서 백그라운드 읽기·활성화·최소화 복원·대상 변경 및 취소 거부도 통과했습니다. Windows Server 2025 CI에서 두 역할의 빌드·실제 EXE 검사·설치·재설치·제거·설정 보존을 확인했으며, 정식 Release의 공개 다운로드·버전·소스·SHA256 검증까지 완료했습니다. 실제 Win7 KI-Messenger 동작은 현장 확인 전이며, 이 자동 검사들이 실제 Win7 확인을 대신하지 않습니다.
